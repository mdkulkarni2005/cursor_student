{
  "name": "@studentos/email",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Transactional email (Resend), driver-abstracted. EMAIL_DRIVER=stub logs instead of sending — no account needed for local dev.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "verify": "tsx scripts/verify.ts"
  },
  "dependencies": {
    "resend": "^4.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "tsx": "^4.19.2",
    "typescript": "^5.9.3"
  }
}
