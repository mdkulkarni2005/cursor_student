{
  "name": "@studentos/execution",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Code execution layer — runs user solutions in a sandbox (Piston), provider-abstracted. Fails CLOSED: an unreachable engine is reported 'unverified', never as a pass.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "verify": "tsx scripts/verify.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.19.43",
    "tsx": "^4.19.2",
    "typescript": "^5.9.3"
  }
}
