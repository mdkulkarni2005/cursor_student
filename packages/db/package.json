{
  "name": "@studentos/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Prisma schema + client for StudentOS (Neon Postgres).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "studio": "prisma studio",
    "push": "prisma db push",
    "format": "prisma format",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/client": "^6.2.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "prisma": "^6.2.1",
    "typescript": "^5.9.3"
  }
}
