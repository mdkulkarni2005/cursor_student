{
  "name": "@studentos/api-types",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "RN-safe request/response types + fetch client shared between apps/web's /api/mobile/* routes and apps/mobile. No Prisma, no Node-only deps — must stay importable from Expo.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
